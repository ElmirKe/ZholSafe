/**
 * Android bindings for the core detector: ONNX Runtime {@code TensorSession} and asset-backed
 * model files. Only this package imports {@code ai.onnxruntime.*}. Model semantics (spec,
 * preprocessing, decoding, NMS, label mapping) live in the pure-Java core.
 */
package kz.zholsafe.ai;
