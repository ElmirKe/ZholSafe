/**
 * Local alert system (Stage 1/4): {@code AlertManager} implements
 * {@link kz.zholsafe.pipeline.AlertSink} with audio (ToneGenerator/SoundPool) and visual cues.
 * Must operate with no network, server or GPS.
 */
package kz.zholsafe.alert;
