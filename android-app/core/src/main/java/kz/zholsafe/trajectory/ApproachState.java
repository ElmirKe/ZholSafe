package kz.zholsafe.trajectory;

/** Image-scale evidence only; APPROACHING does NOT assert physical closing or collision. */
public enum ApproachState { APPROACHING, RECEDING, LATERAL_OR_STABLE, UNCERTAIN }
