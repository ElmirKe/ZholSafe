package kz.zholsafe.trajectory;

/** Image direction: +X is right, +Y is DOWN; never a geographic heading. */
public enum ImageDirection {
    LEFT, RIGHT, UP, DOWN, UP_LEFT, UP_RIGHT, DOWN_LEFT, DOWN_RIGHT, STATIONARY, UNCERTAIN
}
