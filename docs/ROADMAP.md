# ZholSafe — Roadmap

Priorities: **P0** road hazard detection, driver state detection, local Risk Engine, local
warning · **P1** tracking, trajectory, risk zone · **P2** GPS hazard events, ZholNet, WebSocket
warning to other vehicles · **P3** risk map, historical analytics, advanced UI.
Never sacrifice a working P0 pipeline for P2/P3 features.

| Stage | Scope | Exit criteria | Status |
|-------|-------|---------------|--------|
| **0.1** | Contract hardening: strict v1 enum validation on the server (UNKNOWN legitimate, malformed rejected), NaN/∞ rejection across numeric contracts, DriverState/TrackedObject/VehicleContext invariants, distance/TTC sign semantics, configuration constructor validation | All tests pass; no architecture change | **DONE** |
| **0** | Architecture & foundation: monorepo, docs, data contracts, enums/interfaces, baseline Risk Engine + contract tests, Spring Boot skeleton with health test, Android skeleton, ai-training scaffold, config placeholders | Core tests pass; server builds & health test passes; docs frozen | **DONE** (this branch) — see Stage 0 report |
| **1** | Android + CameraX road-camera frame pipeline: runtime CAMERA permission, rear-camera `RoadCamera` FrameSource (Preview + ImageAnalysis, KEEP_ONLY_LATEST), pooled `ImageProxy`→NV21 `Frame` adapter, `FramePipeline` (single processing executor), `PipelineTelemetry` + engineering overlay, deterministic `SyntheticFrameSource` DEMO, `FrameProcessor` insertion point for Stage 2 | Core pipeline unit-tested on the JVM (68/68); app sources compiled against API stubs; **Gradle/APK build and on-device run NOT EXECUTED** (no SDK/network in authoring sandbox) — device verification is the first task of Stage 2 | **CODE DONE / DEVICE PENDING** |
| **1.1** | Camera pipeline hardening: `Frame.timestampNanos` = CameraX image timestamp (not callback time), clock-domain documentation, foreground-service permissions removed until the service exists | Core tests pass; Android build still NOT EXECUTED in authoring sandbox | **DONE (code)** |
| **1.x (deferred)** | Driver (front) camera `FrameSource`, foreground service, `AlertManager` audio/visual, Gradle wrapper commit | — | open |
| **2** | RoadGuard detection architecture: `RoadDetector` + `OnnxRoadDetector` (model-agnostic, `ModelSpec`-driven), ONNX Runtime Android boundary (`TensorSession`), NV21 preprocessing with logical rotation + letterbox, `YoloRawDecoder`/`YoloEnd2EndDecoder`, class-aware NMS, label-string `LabelMap` policy, `RoadDetectionProcessor` + `DetectionSnapshot`, engineering UI + overlay, benchmark harness, spec/export tooling for YOLO26n and YOLO11n | 138 JVM tests pass; decoders/preprocessing/label map/pipeline UNIT TESTED; **real ONNX session, Android build and camera inference NOT VERIFIED** (no model binaries, no SDK/network in authoring sandbox); MODEL SELECTION DEFERRED | **CODE DONE / MODEL + DEVICE PENDING** |
| **2.1** | ONNX boundary hardening: canonical `TensorElementType` (FLOAT32 required, explicit ORT enum mapping), ORT adapter audited + compiled against onnxruntime v1.19.2 Java sources, strict integer end2end class index, finite-output policy in both decoders and `RawDetection` | 145 JVM tests pass; ORT adapter API-compatible (compile check); runtime/Android build still NOT VERIFIED | **DONE (code)** |
| **2.5** | Real YOLO11n ONNX end-to-end smoke test on the JVM: official Ultralytics export of yolo11n.pt (weights hash UNCONFIRMED vs upstream — see model-manifest.json), graph inspected, `model-spec.json` → OBSERVED, `:ort-adapter` module (production `OrtTensorSession` shared by app and desktop), `:smoke-test` harness (JPEG→NV21→production detector→real ONNX Runtime), 31 real images, rotation/colour/letterbox checks, Ultralytics reference comparison (79/79 matched, mean IoU 0.959) | All five gates PASS on desktop (`docs/STAGE2_5_REAL_MODEL_TEST.md`); **Android build/device still NOT EXECUTED**; no accuracy claim | **DONE (desktop)** |
| **3** | RoadGuard multi-object tracking: ByteTrack-inspired, class-aware two-pass IoU association, bounded history/lifecycle, `TrackingSnapshot`, same-thread detector integration + track IDs in overlay. **No physical estimates.** | Deterministic core/pipeline tracking tests and regressions; Android remains unverified | **DONE (code/JVM); DEVICE PENDING** — `docs/STAGE3_TRACKING.md` |
| **4.0** | Image-space trajectory: normalized centre velocity and log-area trend from bounded Stage 3 confirmed-track history via multi-sample regression; explicit weak/stale/unavailable semantics; no physical values | Synthetic motion/regression tests and existing core regressions; Android pending | **DONE (code/JVM); DEVICE PENDING** — `docs/STAGE4_0_TRAJECTORY.md` |
| **4.1** | Experimental ground-plane + optional object-height depth, bounded physical range regression, separate metric/optical TTC, explicit quality/failure semantics; **not risk fusion** | Analytic synthetic geometry/temporal/JVM tests and explicit unavailability without calibration; field methodology NOT validated | **DONE (foundation code/JVM); CALIBRATION/DEVICE PENDING** — `docs/STAGE4_1_DISTANCE_TTC.md` |
| **4.2** | Road-only explainable snapshot risk evaluator: normalized trapezoid, image-space entry, quality-aware physical/optical cues, per-track engineering severity and deterministic max frame severity; legacy RiskEngine/RiskInput retained because they cannot carry synchronized Stage 3–4.1 lineage | Synthetic tests; no field-calibrated probability, driver monitoring, production alerts or Android build | **DONE (experimental code/JVM); DEVICE/ROAD VALIDATION PENDING** — `docs/STAGE4_2_RISK_ENGINE.md` |
| **4.3** | DriverGuard: source-time temporal analysis, time-weighted PERCLOS, driver risk and explicit CombinedRisk matrix | JVM regression suite passes, including bilateral-eye hotfix; real driver camera/landmark backend remains unverified | **DONE (code/JVM); DEVICE/BACKEND PENDING** — `docs/STAGE4_3_DRIVERGUARD.md` |
| **5** | ZholNet: Spring Boot + PostgreSQL/PostGIS persistence, `POST /api/v1/hazards`, validation & expiration, `GET /api/v1/hazards/nearby`, migrations, STOMP broadcast | Server unit/MVC/query-contract tests pass; real PostGIS integration pending where Docker is available | implemented; integration pending |
| **6.0** | Android ZholNet foundation: validated foreground location abstraction, local-risk-to-network bridge, Stage 5 contract DTO/schema test, asynchronous HTTP publish/nearby client, bounded metadata retry queue, deterministic synthetic mode | Android debug build + JVM tests pass; local risk remains independent; device and real server integration pending | **DONE (code/build); DEVICE + REAL POSTGIS PENDING** — `docs/STAGE6_0_ANDROID_ZHOLNET_CLIENT.md` |
| **6.1** | Second-vehicle advisory: bounded REST polling, freshness/geodesic/direction evaluation, local event-ID suppression and separate UI panel | Deterministic Vehicle A/B JVM scenario passes; Android device and real PostGIS chain pending | **DONE (code/build); DEVICE + REAL POSTGIS PENDING** — `docs/STAGE6_1_REMOTE_WARNING.md` |
| **6.2** | Final hazard map integration | Evaluated remote events visible on a map without changing local risk semantics | NOT IMPLEMENTED |
| **6.3** | Integrated two-vehicle hackathon demo | Vehicle A publish through real server to Vehicle B advisory | NOT IMPLEMENTED |
| **7** | Final integration, clean build/test matrix, boundedness/privacy/claims audit and deterministic fallback demo | 373 automated tests pass; Android APK builds; device and real PostGIS remain explicitly unverified | **DONE (hackathon stabilization)** — `docs/STAGE7_VERIFICATION_REPORT.md` |

Current sequence: **Stage 3 tracking → 4.0 trajectory → 4.1 distance/TTC foundation → 4.2 road risk → 4.3 DriverGuard/fusion → 5 ZholNet server → 6.0 Android client foundation → 6.1 remote advisory → 6.2 map → 6.3 demo → 7 audit**. Stage 5 real PostGIS runtime verification remains pending where Docker is unavailable. Stage 6.1 uses authoritative REST nearby polling; live STOMP and the final map are not implemented.

## Explicitly out of scope until instructed

C++/JNI, Kotlin, Python inference server, microservices, React or other frontend frameworks,
production authentication, fabricated models or metrics.
