[project]
name = "zholsafe-ai"
version = "0.0.1"
description = "ZholSafe offline AI development utilities (training-time only)"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
